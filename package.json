{
  "name": "mehiron-site",
  "private": true,
  "description": "בדיקות: המודולים הטהורים של האתר (geo.js, alerts.js) ב-Node, ובחירת שמות וקריאת קובצי סניפים ב-Python. אין שלב בנייה.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\" && python -m unittest discover -s tests -p \"test_*.py\""
  }
}
